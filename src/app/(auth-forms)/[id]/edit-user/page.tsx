import EditUserModal from "@/sections/profile/edit-user-modal";

interface Props {
  params: { id: string };
}

export default function EditUserPage({ params: { id } }: Props) {
  //   return <EditUserModal userId={id} />;
}
